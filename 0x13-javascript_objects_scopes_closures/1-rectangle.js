#!/usr/bin/node
/*
 * A class Rectangle that defines a rectangle.
 * constructor arguments:
 *   width = w
 *   height = h
 */

module.exports = class Rectangle {
  constructor (w, h) {
    this.width = w;
    this.height = h;
  }
};
